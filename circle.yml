machine:
  environment:
    GOPATH: "$HOME/.go_workspace:/usr/local/go_workspace:${HOME}/.go_project"

dependencies:
  override:
    - go get -t -d -v ./...
    - go install -v

test:
  override:
    - go test


